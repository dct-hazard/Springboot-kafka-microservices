apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: stock-service
  namespace: amq-streams-kafka
spec:
  output:
    to:
      kind: ImageStreamTag
      name: stock-service:latest
  source:
    type: Git
    git:
      uri: "https://github.com/vaibhava_srivastava/Springboot-kafka-microservices.git"
      ref: "main"
    contextDir: "stock-service"
  strategy:
    type: Docker
    dockerStrategy: {}
  triggers:
    - type: ConfigChange
    - type: ImageChange
